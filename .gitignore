# Security — never commit these
.env
.env.local
.env.*
*.env

# API Keys & Secrets
secrets/
.secrets
api-keys.txt

# Claude Code local settings — contains API keys and auth tokens
.claude/settings.json
.claude/settings.local.json

# Sensitive outputs
outputs/reports/financial-*
state/*.json

# System files
.DS_Store
node_modules/
*.log
