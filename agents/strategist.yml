name: strategist
skills: seasonal-events
# Layer 2: reads prior agent outputs + context files — no state/ access needed
tools: Read(context/seasonal-calendar.md), Read(outputs/**), Read(skills/**), Write(outputs/blueprints/**), Write(state/status.json)
Input required: all outputs/research/ files (run other agents first)
Output: outputs/blueprints/[campaign]-blueprint.md
End: update state/status.json = blueprint-ready → print "BLUEPRINT READY — awaiting review"
